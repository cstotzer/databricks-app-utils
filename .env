# PAT auth (local/dev)
DATABRICKS_SERVER_HOSTNAME=dbc-dbb3a612-eed0.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/be8842aa84e5d7da

# For PAT auth, set the token directly in the env (only for dbx4dev))
# DATABRICKS_AUTH_METHOD=pat
DATABRICKS_PAT=dapi...

# Alternative: OAuth U2M (interactive, may open browser / rely on cached token)
DATABRICKS_AUTH_METHOD=u2m_persistent

# Alternative: OBO (Databricks Apps only; requires X-Forwarded-Access-Token header)
# DATABRICKS_AUTH_METHOD=obo

DATABRICKS_DEFAULT_CATALOG=samples
DATABRICKS_DEFAULT_SCHEMA=bakehouse